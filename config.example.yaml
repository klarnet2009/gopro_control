server:
  host: 127.0.0.1
  port: 8000

poll_interval_sec: 2

cameras:
  - id: cam-front
    name: "Front Camera"
    target: "1234"      # last 4 chars of camera serial (Settings → About → Camera Info)
    mode: "ble"         # ble (fast, start/stop) | cohn (HTTP over Wi-Fi, clock sync, preview)
  - id: cam-side
    name: "Side Camera"
    target: "5678"
    mode: "ble"
